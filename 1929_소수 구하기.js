const fs = require('fs');
const input = fs.readFileSync('./dev/stdin').toString().trim().split('\n');
const arr= input[i].split(' ')
   const M = parseInt(arr[0]); //start
   const N = parseInt(arr[1]); //end

   for (let i=M; i<=N; i++) {
      
   }